{
  "name": "templater-website",
  "version": "1.0.0",
  "description": "Website that helps you design custom websites from the templates in an instant",
  "main": "server/server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server/server.js",
    "start": "node server/server.js",
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tailwindcss/vite": "^4.0.2",
    "bcrypt": "^5.1.1",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.10.0",
    "tailwindcss": "^4.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "keywords": []
}
